/** Moved to the canonical theme at `src/theme/`. Kept as a re-export so existing
 *  `../constants/colors` imports keep working. New code should import from `src/theme`. */
export * from '../theme/colors';
